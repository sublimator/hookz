[project]
name = "tipbot-tests"
version = "0.1.0"
description = "Tipbot hook tests — example hookz project"
requires-python = ">=3.12"
dependencies = [
    "hookz",
    "pytest>=8",
]

[tool.uv.sources]
hookz = { path = "../../", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
